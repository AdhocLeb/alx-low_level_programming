#include "main.h"
/**
 * main - program that prints _putchar, followed by a new line.
 * Return: 0 Always
 */
int main(void)
{
	int in = 97;

	while (in <= 122)
	{
		putchar(in);
		in++;
	}
	putchar('\n');

return (0);
}
