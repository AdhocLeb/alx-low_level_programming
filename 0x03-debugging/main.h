#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

/* function to check if the given integer is positive or negative */
void positive_or_negative(int i);

/* function to find the largest number among three integers */
int largest_number(int a, int b, int c);

/* function to convert the given day and month into day of the year */
int convert_day(int month, int day);

/* function to print the remaining days in the year from the given date */
void print_remaining_days(int month, int day, int year);

#endif

